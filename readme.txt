The requirements.txt is located within the directory, i used the weather-data directory for the csv because that's where
it unpacked to and i find it unpleasant to have the data sitting around in the same directory as the code.
The hidden and malformed files tho should by default be placed in the same directory as the notebook as per instructions
The requirements are only the dateutil and matplotlib for plotting. As they are so standard i didn't really bother with a
specific version.

pip install -r requirements.txt

as per usual if necessary.